version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: mongo-order-lifecycle
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: orders

volumes:
  mongo_data:
